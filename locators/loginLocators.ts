// We use the 'export' keyboard as it allows us to access the objects outside a class
export const LoginLocators =
    {
        usernameInput: "#user-name",
        passwordInput: "#password",
        loginButton: "#login-button"
    }